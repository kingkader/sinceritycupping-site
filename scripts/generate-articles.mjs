// Automatic article rendering is retired. New or changed articles require
// direct static edits to the article, manifest, topic data, sitemap and LLM
// files, followed by tests and human review.
console.error("Automatic article rendering is retired. New or changed articles require direct static edits to the article, manifest, topic data, sitemap and LLM files, followed by tests and human review.");
process.exit(1);
